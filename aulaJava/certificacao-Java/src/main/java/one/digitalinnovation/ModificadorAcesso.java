package one.digitalinnovation;

public class ModificadorAcesso {
    public static void main(String[] args){
        //public pode ser acessado de qualquer lugar e qualquer entidade que possa visualizar a classe que ela pertence;
        //private os metodos e atributos nao podem ser acessados ou usados por nenhuma outra classe, nem pelas classes herdadas;
        //protected acessivel as classes do mesmo pacote,ou atraves de herança do mesmo pacote;
        //default só é acessivel pelo mesmo pacote
        //abstract aplicado apenas nos classes e metodos, nao pode ser instanciada;
        //static é usado para criacao de variaveis, afeta todas as instancias se mudar a variavel;
        //final nao permite estender uma classe, sobrescrever(overrinddg) um metodo, e nao poder alterar o valor da variavel;
        
    }
}
